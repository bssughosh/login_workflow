import 'package:flutter/material.dart';

class PasswordDisplayStateViewMobile extends StatelessWidget {
  final bool isWeb;

  const PasswordDisplayStateViewMobile({Key? key, this.isWeb = false})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
